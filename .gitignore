__pycache__/
*.pyc
.venv/
venv/
.env
*.egg-info/

# artefatos grandes de treino (não versionar no git)
finetuning/train/checkpoints/
finetuning/train/adapters/
*.gguf
*.safetensors
*.bin

# índices vetoriais (gerados via app/core/ingestion.py)
data/vectorstore/

# logs de auditoria (gerados em runtime)
data/logs/

# roteiro do vídeo — só local, não vai pro repo público
examples/ROTEIRO_VIDEO.md

# terraform (o .terraform.lock.hcl FICA versionado, de propósito)
infra/**/.terraform/
infra/**/*.tfstate
infra/**/*.tfstate.backup
infra/**/*.secrets.tfvars
infra/**/crash.log
infra/**/tfplan
infra/**/*.tfplan
